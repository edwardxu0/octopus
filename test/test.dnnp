from dnnv.properties import *
import numpy as np

N = Network("N")
x = Image("./test/test.npy")
epsilon = 0.02
Forall(
    x_,
    Implies(
        ((x - epsilon) < x_ < (x + epsilon)),
        np.argmax(N[:](x_)) == np.argmax(N[:](x)),
    ),
)
